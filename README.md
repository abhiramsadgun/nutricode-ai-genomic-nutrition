# NutriCode — DNA & AI-based Diet Generator (Flask)

A working demo: login, DNA sample registration, PDF health report analysis with numeric thresholds, personalized diets, contact doctor, and a safe triage Health Chat.

## Quickstart
```bash
cd backend
python -m venv .venv
# Windows: .venv\Scripts\Activate.ps1
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
flask --app app.py db-init
flask --app app.py run
```
Login: `admin@example.com` / `Pass@123`
